# 🛠️ План исправления проблем безопасности ToDoLite v1.5.2

**Дата создания:** 13 октября 2025  
**Версия:** v1.5.2  
**Автор:** AI Assistant  
**Тип:** Security Fix Plan

## 🎯 Цель

Исправить найденные проблемы безопасности и уязвимости в ToDoLite без нарушения функциональности.

## 📋 План исправлений

### 🔴 Критические проблемы (Приоритет 1)

#### 1. Добавить аутентификацию
**Проблема:** Приложение доступно всем пользователям локальной сети

**Решение:**
```python
# Создать модуль auth.py
class SimpleAuth:
    def __init__(self):
        self.users = {'admin': 'password123'}  # Временно
    
    def check_auth(self, username, password):
        return self.users.get(username) == password
    
    def require_auth(self, f):
        @wraps(f)
        def decorated(*args, **kwargs):
            auth = request.authorization
            if not auth or not self.check_auth(auth.username, auth.password):
                return Response('Требуется аутентификация', 401,
                              {'WWW-Authenticate': 'Basic realm="ToDoLite"'})
            return f(*args, **kwargs)
        return decorated
```

**Файлы для изменения:**
- `app.py` - добавить декораторы аутентификации
- `auth.py` - новый модуль аутентификации
- `config.json` - добавить настройки пользователей

#### 2. Убрать заглушки и добавить логирование
**Проблема:** 22 заглушки `pass` в блоках `except`

**Решение:**
```python
# Заменить все заглушки на логирование
try:
    # код
except Exception as e:
    logger.error(f"Ошибка в функции: {e}", "ERROR")
    # Возврат безопасного значения или повторный вызов
```

**Файлы для изменения:**
- `notifications_windows.py` - 5 заглушек
- `tray_app.py` - 8 заглушек  
- `app.py` - 1 заглушка
- `reminder_manager.py` - 1 заглушка

### 🟡 Средние проблемы (Приоритет 2)

#### 3. Добавить CSRF защиту
**Проблема:** Отсутствие защиты от CSRF атак

**Решение:**
```python
# Добавить CSRF токены
from flask_wtf.csrf import CSRFProtect

app = Flask(__name__)
app.config['SECRET_KEY'] = 'your-secret-key'
csrf = CSRFProtect(app)

# В шаблонах добавить токены
<form method="POST">
    <input type="hidden" name="csrf_token" value="{{ csrf_token() }}"/>
    <!-- остальные поля -->
</form>
```

#### 4. Улучшить обработку файлов
**Проблема:** Отсутствие проверки MIME типов и размеров

**Решение:**
```python
# Создать модуль file_utils.py
class FileValidator:
    ALLOWED_EXTENSIONS = {'.txt', '.md', '.csv', '.json'}
    MAX_FILE_SIZE = 10 * 1024 * 1024  # 10MB
    
    def validate_file(self, file):
        if not file:
            return False, "Файл не выбран"
        
        if file.content_length > self.MAX_FILE_SIZE:
            return False, "Файл слишком большой"
        
        ext = os.path.splitext(file.filename)[1].lower()
        if ext not in self.ALLOWED_EXTENSIONS:
            return False, "Недопустимый тип файла"
        
        return True, "OK"
```

#### 5. Исправить информационные утечки
**Проблема:** Подробные сообщения об ошибках в логах

**Решение:**
```python
# Создать модуль error_handler.py
class ErrorHandler:
    def __init__(self, debug_mode=False):
        self.debug_mode = debug_mode
    
    def handle_error(self, error, context=""):
        if self.debug_mode:
            logger.error(f"Ошибка в {context}: {error}", "ERROR")
        else:
            logger.error(f"Ошибка в {context}: [скрыто]", "ERROR")
            # Возврат пользователю общее сообщение
```

### 🟢 Низкие проблемы (Приоритет 3)

#### 6. Добавить rate limiting
**Проблема:** Отсутствие ограничений на количество запросов

**Решение:**
```python
# Добавить rate limiting
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address

limiter = Limiter(
    app,
    key_func=get_remote_address,
    default_limits=["100 per hour"]
)

@app.route('/add_task', methods=['POST'])
@limiter.limit("10 per minute")
def add_task_route():
    # код
```

#### 7. Добавить security headers
**Проблема:** Отсутствие защитных заголовков

**Решение:**
```python
# Добавить security headers
@app.after_request
def add_security_headers(response):
    response.headers['X-Content-Type-Options'] = 'nosniff'
    response.headers['X-Frame-Options'] = 'DENY'
    response.headers['X-XSS-Protection'] = '1; mode=block'
    response.headers['Strict-Transport-Security'] = 'max-age=31536000'
    return response
```

## 📅 Временной план

### Неделя 1: Критические проблемы
- **День 1-2:** Добавить аутентификацию
- **День 3-4:** Убрать заглушки и добавить логирование
- **День 5:** Тестирование критических исправлений

### Неделя 2: Средние проблемы
- **День 1-2:** Добавить CSRF защиту
- **День 3-4:** Улучшить обработку файлов
- **День 5:** Исправить информационные утечки

### Неделя 3: Низкие проблемы
- **День 1-2:** Добавить rate limiting
- **День 3-4:** Добавить security headers
- **День 5:** Финальное тестирование

## 🧪 План тестирования

### Тесты безопасности
1. **Аутентификация**
   - Проверка доступа без логина
   - Проверка доступа с неверными данными
   - Проверка доступа с правильными данными

2. **CSRF защита**
   - Попытка отправки формы без токена
   - Попытка отправки формы с неверным токеном
   - Проверка работы с правильным токеном

3. **Обработка файлов**
   - Загрузка файлов недопустимых типов
   - Загрузка файлов превышающих размер
   - Загрузка корректных файлов

### Тесты функциональности
1. **Основные функции**
   - Создание/редактирование задач
   - Резервное копирование
   - Система напоминаний

2. **Производительность**
   - Время отклика после изменений
   - Использование памяти
   - Нагрузочное тестирование

## 📊 Метрики успеха

### Безопасность
- **Критические уязвимости:** 0
- **Средние уязвимости:** 0
- **Низкие уязвимости:** 0
- **Заглушки в коде:** 0

### Функциональность
- **Потеря функций:** 0%
- **Новые функции:** +5%
- **Стабильность:** +20%

### Производительность
- **Время отклика:** ±0%
- **Использование памяти:** +5%
- **Время запуска:** +10%

## 🚨 Риски и митигация

### Высокие риски
1. **Нарушение функциональности**
   - **Митигация:** Тщательное тестирование
   - **План Б:** Откат изменений

2. **Проблемы с производительностью**
   - **Митигация:** Профилирование кода
   - **План Б:** Оптимизация критических участков

### Средние риски
3. **Сложность внедрения**
   - **Митигация:** Поэтапное внедрение
   - **План Б:** Упрощение решений

4. **Совместимость**
   - **Митигация:** Обратная совместимость
   - **План Б:** Миграционные скрипты

## 📋 Чек-лист выполнения

### Критические проблемы
- [ ] Добавить аутентификацию
- [ ] Убрать все заглушки `pass`
- [ ] Добавить логирование ошибок
- [ ] Протестировать аутентификацию

### Средние проблемы
- [ ] Добавить CSRF защиту
- [ ] Улучшить обработку файлов
- [ ] Исправить информационные утечки
- [ ] Протестировать CSRF защиту

### Низкие проблемы
- [ ] Добавить rate limiting
- [ ] Добавить security headers
- [ ] Протестировать производительность
- [ ] Финальное тестирование

---

**Статус:** 📋 План готов к выполнению  
**Следующий этап:** Начало реализации исправлений
