# 📋 План реализации автоматического перемещения задач по категориям на основе дат

**Дата создания:** 10 ноября 2025  
**Версия:** v1.5.6 → v1.5.7  
**Тип:** Feature Enhancement

---

## 🎯 Цель

Реализовать систему автоматического перемещения задач между категориями на основе дат выполнения/отложенности, чтобы задачи автоматически попадали в соответствующие категории в зависимости от временной близости.

---

## 📊 Текущее состояние

### Категории в config.json:
```json
{
  "statuses_order": [
    "new",      // 🆕 Новые
    "later",    // ⏰ Завтра
    "tracking", // 👀 Отслеживаем
    "working",  // ⚡ Сегодня
    "waiting",  // 🤔 На неделе
    "think",    // ⏳ Далекие
    "done",     // ✅ Готово
    "cancelled" // ❌ Отменено
  ]
}
```

### Поля дат в базе данных:
- `scheduled_date` (DATE) - дата отложенности/планирования
- `due_date` (DATE) - дата выполнения/дедлайн

---

## 🎯 Требования

### 1. Логика определения категории по дате

#### Приоритет дат:
1. Использовать `due_date` если она установлена
2. Если `due_date` отсутствует, использовать `scheduled_date`
3. Если обе даты отсутствуют, задача остаётся в текущей категории

#### Правила категоризации:

**"Сегодня" (working):**
- `due_date` или `scheduled_date` = сегодня
- Просроченные задачи (дата < сегодня) → переместить в "Сегодня"

**"Завтра" (later):**
- `due_date` или `scheduled_date` = завтра

**"На неделе" (waiting):**
- `due_date` или `scheduled_date` в пределах текущей недели (понедельник-воскресенье)
- **Особый случай:** Если сегодня пятница, то включать также ближайший понедельник

**"Далекие" (think):**
- `due_date` или `scheduled_date` дальше чем вторник следующей недели
- То есть: дата > вторник следующей недели

**"Новые" (new):**
- Задачи без дат остаются в "Новых"
- Задачи с датами автоматически перемещаются

### 2. Правила автоматического перемещения

#### Категории, из которых можно перемещать:
- ✅ "Новые" (new)
- ✅ "Далекие" (think)
- ✅ "На неделе" (waiting)
- ✅ "Завтра" (later)
- ✅ "Сегодня" (working) - только для просроченных задач

#### Категории, из которых НЕ перемещать:
- ❌ "Отслеживаем" (tracking) - пользователь сам управляет этими задачами
- ❌ "Сегодня" (working) - если задача не просрочена (пользователь может временно взять задачу в работу)

#### Исключения:
- Просроченные задачи из "Отслеживаем" → переместить в "Сегодня"
- Просроченные задачи из "Сегодня" → остаются в "Сегодня"

### 3. Технические требования

#### Таймер проверки:
- Интервал: 30 минут (настраивается в config.json)
- Запуск: при старте приложения (tray_app.py или app.py)
- Фоновый процесс: не блокирует работу приложения

#### Конфигурация:
```json
{
  "auto_migration": {
    "enabled": true,
    "interval_minutes": 30
  }
}
```

#### Обновление смайликов:
- "🆕 Новые" → "📝 Новые" или "✨ Новые"
- "⏰ Завтра" → "📅 Завтра" или "⏭️ Завтра"
- "👀 Отслеживаем" → оставить или "👁️ Отслеживаем"
- "⚡ Сегодня" → "🔥 Сегодня" или "⚡ Сегодня" (оставить)
- "🤔 На неделе" → "📆 На неделе" или "🗓️ На неделе"
- "⏳ Далекие" → "🔮 Далекие" или "📅 Далекие"

---

## 🏗️ Архитектура решения

### 1. Новый модуль: `category_migration_manager.py`

**Ответственность:**
- Определение категории задачи по дате
- Автоматическое перемещение задач между категориями
- Запуск периодической проверки

**Основные функции:**

```python
class CategoryMigrationManager:
    def __init__(self, db_path='tasks.db', config_manager=None):
        """Инициализация менеджера миграции категорий"""
        
    def get_category_by_date(self, due_date, scheduled_date):
        """Определяет категорию задачи на основе дат"""
        # Возвращает: 'working', 'later', 'waiting', 'think', или None
        
    def is_overdue(self, date):
        """Проверяет, просрочена ли задача"""
        
    def get_tuesday_next_week(self):
        """Вычисляет дату вторника следующей недели"""
        
    def is_in_current_week(self, date):
        """Проверяет, находится ли дата в текущей неделе"""
        
    def should_migrate_task(self, task_status):
        """Проверяет, можно ли перемещать задачу из текущей категории"""
        
    def migrate_tasks(self):
        """Выполняет миграцию всех задач, которые нужно переместить"""
        # 1. Получить все задачи с датами
        # 2. Для каждой задачи определить целевую категорию
        # 3. Проверить, можно ли перемещать
        # 4. Выполнить перемещение если нужно
        
    def start_scheduler(self, interval_minutes=30):
        """Запускает периодическую проверку"""
```

### 2. Интеграция в существующие модули

#### `app.py`:
- Импортировать `CategoryMigrationManager`
- Запускать при старте Flask приложения (если не в режиме трея)

#### `tray_app.py`:
- Импортировать `CategoryMigrationManager`
- Запускать при старте приложения в трее
- Обеспечить остановку при выходе из приложения

#### `config_manager.py`:
- Добавить поддержку секции `auto_migration` в конфигурации
- Предоставить значения по умолчанию

### 3. Обновление конфигурации

#### `config.json`:
```json
{
  "version": "1.5.6",
  "statuses_labels": {
    "new": "📝 Новые",
    "later": "📅 Завтра",
    "tracking": "👁️ Отслеживаем",
    "working": "🔥 Сегодня",
    "waiting": "📆 На неделе",
    "think": "🔮 Далекие",
    "done": "✅ Готово",
    "cancelled": "❌ Отменено"
  },
  "auto_migration": {
    "enabled": true,
    "interval_minutes": 30
  }
}
```

---

## 📝 План реализации (пошагово)

### Этап 1: Подготовка и анализ
- [x] Анализ текущей структуры категорий
- [x] Анализ использования дат в задачах
- [x] Составление плана реализации

### Этап 2: Создание модуля миграции категорий
- [x] Создать файл `category_migration_manager.py`
- [x] Реализовать функцию `get_category_by_date()`:
  - [x] Логика определения "Сегодня"
  - [x] Логика определения "Завтра"
  - [x] Логика определения "На неделе" (с учётом пятницы)
  - [x] Логика определения "Далекие" (вторник следующей недели)
- [x] Реализовать функцию `is_overdue()`
- [x] Реализовать функцию `should_migrate_task()`
- [x] Реализовать функцию `migrate_tasks()`
- [x] Реализовать функцию `start_scheduler()`
- [x] Реализовать функцию `migrate_tasks_async()` для ручного запуска

### Этап 3: Обновление конфигурации
- [x] Обновить `config_manager.py` для поддержки `auto_migration`
- [x] Обновить `config.json` с новыми смайликами
- [x] Обновить значения по умолчанию в `config_manager.py`

### Этап 4: Интеграция
- [x] Интегрировать в `app.py` (запуск при старте Flask)
- [x] Интегрировать в `tray_app.py` (запуск при старте трея)
- [x] Обеспечить корректную остановку при выходе
- [x] Добавить кнопку ручного запуска миграции в интерфейс
- [x] Создать маршрут `/migrate_tasks` для ручного запуска

### Этап 5: Тестирование
- [x] Тестирование логики определения категорий:
  - [x] Задачи на сегодня
  - [x] Задачи на завтра
  - [x] Задачи на этой неделе
  - [x] Задачи на следующей неделе (пятница → понедельник)
  - [x] Просроченные задачи
  - [x] Задачи без дат
- [x] Тестирование правил перемещения:
  - [x] Из "Новых"
  - [x] Из "Далеких"
  - [x] Из "На неделе"
  - [x] Из "Завтра"
  - [x] НЕ из "Отслеживаем" (кроме просроченных)
  - [x] НЕ из "Сегодня" (кроме просроченных)
- [x] Тестирование таймера (30 минут)
- [x] Тестирование на граничных случаях
- [x] Тестирование ручного запуска миграции

### Этап 6: Документация
- [x] Обновить `README.md` с описанием новой функции
- [x] Добавить описание в `CLIENT_README.md`
- [x] Обновить журнал разработки

---

## 🔍 Детальная логика определения категорий

### Алгоритм `get_category_by_date(due_date, scheduled_date)`:

```python
def get_category_by_date(due_date, scheduled_date):
    # 1. Определить используемую дату
    target_date = due_date if due_date else scheduled_date
    if not target_date:
        return None  # Нет даты - не перемещать
    
    # 2. Преобразовать в объект date
    if isinstance(target_date, str):
        target_date = datetime.strptime(target_date, '%Y-%m-%d').date()
    today = datetime.now().date()
    
    # 3. Проверить просроченность
    if target_date < today:
        return 'working'  # Просроченные → Сегодня
    
    # 4. Сегодня
    if target_date == today:
        return 'working'
    
    # 5. Завтра
    tomorrow = today + timedelta(days=1)
    if target_date == tomorrow:
        return 'later'
    
    # 6. На неделе
    # Получить начало недели (понедельник)
    days_since_monday = today.weekday()  # 0 = понедельник
    week_start = today - timedelta(days=days_since_monday)
    week_end = week_start + timedelta(days=6)  # Воскресенье
    
    # Если сегодня пятница, включить следующий понедельник
    if today.weekday() == 4:  # Пятница
        next_monday = week_end + timedelta(days=1)
        if week_start <= target_date <= next_monday:
            return 'waiting'
    else:
        if week_start <= target_date <= week_end:
            return 'waiting'
    
    # 7. Далекие (дальше чем вторник следующей недели)
    # Найти вторник следующей недели
    next_week_start = week_end + timedelta(days=1)  # Понедельник следующей недели
    next_tuesday = next_week_start + timedelta(days=1)  # Вторник следующей недели
    
    if target_date > next_tuesday:
        return 'think'
    
    # 8. Между завтра и концом недели (или до вторника следующей недели)
    # Это тоже "На неделе"
    if tomorrow < target_date <= next_tuesday:
        return 'waiting'
    
    return None
```

### Алгоритм `should_migrate_task(task_status, is_overdue)`:

```python
def should_migrate_task(task_status, is_overdue):
    # Категории, из которых можно перемещать
    migratable_statuses = ['new', 'think', 'waiting', 'later']
    
    if task_status in migratable_statuses:
        return True
    
    # Просроченные задачи можно перемещать даже из "tracking" и "working"
    if is_overdue:
        return True
    
    # "tracking" и "working" (не просроченные) - не перемещать
    if task_status in ['tracking', 'working']:
        return False
    
    return False
```

---

## ⚠️ Потенциальные проблемы и решения

### Проблема 1: Конфликт с ручным перемещением задач
**Решение:** 
- Логировать все автоматические перемещения
- Пользователь может вручную вернуть задачу в нужную категорию
- Задачи в "Отслеживаем" защищены от автоматического перемещения

### Проблема 2: Частые перемещения задач
**Решение:**
- Проверка выполняется только раз в 30 минут
- Перемещение выполняется только если категория действительно изменилась
- Логирование для отладки

### Проблема 3: Производительность при большом количестве задач
**Решение:**
- Оптимизированные SQL запросы
- Индексы на полях `due_date` и `scheduled_date`
- Пакетное обновление задач

### Проблема 4: Часовые пояса
**Решение:**
- Использовать локальное время сервера
- Все даты хранить в формате DATE без времени

---

## 📊 Метрики успеха

- ✅ Задачи автоматически перемещаются в правильные категории
- ✅ Просроченные задачи попадают в "Сегодня"
- ✅ Задачи в "Отслеживаем" не перемещаются автоматически (кроме просроченных)
- ✅ Таймер работает стабильно каждые 30 минут
- ✅ Нет конфликтов с ручным управлением задачами
- ✅ Производительность не страдает при большом количестве задач

---

## 🔄 Следующие шаги

1. Начать с реализации модуля `category_migration_manager.py`
2. Протестировать логику определения категорий на тестовых данных
3. Интегрировать в приложение
4. Протестировать в реальных условиях
5. Обновить документацию

---

*План будет обновляться по мере реализации*

